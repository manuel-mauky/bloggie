graphql:
  servlet:
    mapping: /api/graphql
    enabled: true
    corsEnabled: true

spring:
  jpa:
    properties:
      hibernate.enable_lazy_load_no_trans: true

management:
  server:
    port: 8090
  endpoints:
    web:
      exposure:
        include: "*"
